# 2026-03-28 打包前功能冻结清单

## 冻结范围

本轮功能冻结只覆盖当前主线要求：

- 微信维持 `draft` 写入草稿
- 知乎 / 头条号 / 一点号 / 简书达到“正式发布成功”
- 头条号额外达到“按指定时间定时发布成功”
- 托管浏览器会话支持重启后自动补齐平台标签页

## 功能冻结结论

| 项目 | 结论 | 说明 |
| --- | --- | --- |
| 知乎正式发布 | 通过 | 真实站点验收通过 |
| 头条号正式发布 | 通过 | 真实站点验收通过 |
| 头条号定时发布 | 通过 | 真实站点验收返回 `scheduled` |
| 一点号正式发布 | 通过 | 真实站点验收通过 |
| 简书正式发布 | 通过 | 不走封面 / AI 声明，最小发布链路通过 |
| 微信草稿链路 | 保持现状 | 仍按产品边界保留为草稿 |
| 托管浏览器重启补标签页 | 通过 | 真实会话复核通过 |

## 本轮收口点

- `scheduled_publish_at` 已打通桌面状态、bridge、引擎模型、runner、头条脚本和成功状态归类。
- 头条号定时发布已适配真实下拉控件与当前页面按钮流转，不再误走立即发布路径。
- 头条号封面上传已兼容动态上传控件、确认按钮延迟启用，以及无效占位图过滤。
- 简书已从封面分配链路剔除，避免规划层错误注入不支持的封面参数。
- 浏览器托管会话重启后的标签页自动恢复 / 自动补齐已收口。

## 验证依据

- 工程回归：
  - `python3 -m unittest tests.test_engine_assignments tests.test_engine_workbench_bridge tests.test_publish_preflight tests.test_browser_publishers_behavior`
- 真实验收：
  - `docs/manual-validation/2026-03-28-browser-smoke.md`
  - `docs/manual-validation/2026-03-28-browser-session.md`

## 冻结后的下一阶段

功能主线达到当前冻结目标后，后续优先级建议切到：

1. 桌面正式打包与安装链路
2. 运行内存与安装包体积优化
3. 更长时段的稳定性 soak / 风控边界观察
4. UI 细节修整

## 仍未纳入本轮冻结的事项

- DMG 安装器与 macOS 代码签名/公证（当前已实现独立内嵌 Python 的 `.app`，但未解决分发信任链）
- Windows 正式分发、签名与安装器
- 完整断点续跑体系
- 密钥与本地数据的产品级加密与存储治理
